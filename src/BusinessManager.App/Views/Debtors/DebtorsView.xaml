<UserControl x:Class="BusinessManager.App.Views.Debtors.DebtorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="800" d:DesignWidth="1200"
             Loaded="DebtorsView_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <materialDesign:ColorZone Grid.Row="0"
                                  Mode="PrimaryMid"
                                  Padding="16"
                                  Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Debtors (Credit Customers)"
                               Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                               Foreground="White"/>
                    <TextBlock Text="{Binding TotalOutstanding, StringFormat='Total outstanding: {0:N0} UGX'}"
                               Foreground="White"
                               Opacity="0.9"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Style="{StaticResource MaterialDesignIconButton}"
                        Command="{Binding RefreshCommand}"
                        Foreground="White">
                    <materialDesign:PackIcon Kind="Refresh"/>
                </Button>
            </Grid>
        </materialDesign:ColorZone>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="360"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <materialDesign:Card Grid.Row="0" Margin="0,0,0,8">
                    <StackPanel Margin="16">
                        <TextBlock Text="Amount to collect per person"
                                   Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                                   Margin="0,0,0,8"/>
                        <DataGrid ItemsSource="{Binding CustomerSummaries}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  IsReadOnly="True"
                                  HeadersVisibility="Column"
                                  Style="{StaticResource MaterialDesignDataGrid}"
                                  MaxHeight="180">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="*"/>
                                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120"/>
                                <DataGridTextColumn Header="Records" Binding="{Binding OpenRecords}" Width="70"/>
                                <DataGridTextColumn Header="Amount Due" Binding="{Binding TotalOwed, StringFormat='{}{0:N0}'}" Width="120"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </materialDesign:Card>

                <Grid Grid.Row="1" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0"
                             materialDesign:HintAssist.Hint="Search by name, phone, or item..."
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,8,0"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Return" Command="{Binding SearchCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    <Button Grid.Column="1"
                            Content="Search"
                            Command="{Binding SearchCommand}"
                            Style="{StaticResource MaterialDesignOutlinedButton}"/>
                </Grid>

                <materialDesign:Card Grid.Row="2">
                    <DataGrid ItemsSource="{Binding Debtors}"
                              SelectedItem="{Binding SelectedDebtor}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              Style="{StaticResource MaterialDesignDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date" Binding="{Binding RecordDate, StringFormat='{}{0:dd/MM/yyyy}'}" Width="90"/>
                            <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="120"/>
                            <DataGridTextColumn Header="Item / Service" Binding="{Binding Description}" Width="*"/>
                            <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount, StringFormat='{}{0:N0}'}" Width="90"/>
                            <DataGridTextColumn Header="Paid" Binding="{Binding AmountPaid, StringFormat='{}{0:N0}'}" Width="90"/>
                            <DataGridTextColumn Header="Balance" Binding="{Binding Balance, StringFormat='{}{0:N0}'}" Width="90"/>
                            <DataGridTemplateColumn Header="Status" Width="90">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="8" Padding="7,2" Margin="2" HorizontalAlignment="Left">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#FEF3C7"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSettled}" Value="True">
                                                            <Setter Property="Background" Value="#D1FAE5"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock FontSize="11" FontWeight="SemiBold">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Text" Value="OWING"/>
                                                        <Setter Property="Foreground" Value="#92400E"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsSettled}" Value="True">
                                                                <Setter Property="Text" Value="SETTLED"/>
                                                                <Setter Property="Foreground" Value="#065F46"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Actions" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Style="{StaticResource MaterialDesignIconButton}"
                                                    Command="{Binding DataContext.ShowPaymentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    ToolTip="Record Payment">
                                                <materialDesign:PackIcon Kind="CashPlus"/>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </materialDesign:Card>
            </Grid>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="LightGray"/>

            <materialDesign:Card Grid.Column="2">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="16">
                        <TextBlock Text="Add Credit Record"
                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                   Margin="0,0,0,16"/>

                        <TextBox materialDesign:HintAssist.Hint="Customer name"
                                 Text="{Binding CustomerName, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0,0,0,8"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

                        <TextBox materialDesign:HintAssist.Hint="Phone (optional)"
                                 Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0,0,0,8"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

                        <TextBox materialDesign:HintAssist.Hint="What they took (binding, printing, etc.)"
                                 Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0,0,0,8"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

                        <TextBox materialDesign:HintAssist.Hint="Amount owed"
                                 Text="{Binding TotalAmount, Converter={StaticResource CurrencyConverter}, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0,0,0,8"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

                        <DatePicker materialDesign:HintAssist.Hint="Date"
                                    SelectedDate="{Binding RecordDate}"
                                    Margin="0,0,0,8"
                                    Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>

                        <TextBox materialDesign:HintAssist.Hint="Notes (optional)"
                                 Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0,0,0,16"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

                        <Button Content="Save Credit Record"
                                Command="{Binding SaveDebtCommand}"
                                Style="{StaticResource MaterialDesignRaisedButton}"
                                Margin="0,0,0,16"/>

                        <Border Background="{DynamicResource MaterialDesignDividerBackground}"
                                CornerRadius="4"
                                Padding="12"
                                Visibility="{Binding ShowPaymentForm, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel>
                                <TextBlock Text="Record Payment"
                                           Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                                           Margin="0,0,0,8"/>
                                <TextBlock Text="{Binding SelectedDebtor.CustomerName, StringFormat='Customer: {0}'}"
                                           Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding SelectedDebtor.Balance, StringFormat='Balance: {0:N0} UGX'}"
                                           Margin="0,0,0,8"/>
                                <TextBox materialDesign:HintAssist.Hint="Payment amount"
                                         Text="{Binding PaymentAmount, Converter={StaticResource CurrencyConverter}, UpdateSourceTrigger=PropertyChanged}"
                                         Margin="0,0,0,8"
                                         Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                                <TextBox materialDesign:HintAssist.Hint="Payment notes (optional)"
                                         Text="{Binding PaymentNotes, UpdateSourceTrigger=PropertyChanged}"
                                         Margin="0,0,0,8"
                                         Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Save Payment"
                                            Command="{Binding RecordPaymentCommand}"
                                            Style="{StaticResource MaterialDesignRaisedButton}"
                                            Margin="0,0,8,0"/>
                                    <Button Content="Cancel"
                                            Command="{Binding CancelPaymentCommand}"
                                            Style="{StaticResource MaterialDesignOutlinedButton}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>
