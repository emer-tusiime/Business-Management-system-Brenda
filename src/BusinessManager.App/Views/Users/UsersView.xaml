<UserControl x:Class="BusinessManager.App.Views.Users.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="800" d:DesignWidth="1200"
             Loaded="UsersView_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <materialDesign:ColorZone Grid.Row="0" 
                                  Mode="PrimaryMid" 
                                  Padding="16"
                                  Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0"
                           Text="User Management"
                           Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                           Foreground="White"/>
                
                <StackPanel Grid.Column="1" 
                            Orientation="Horizontal">
                    <Button Style="{StaticResource MaterialDesignIconButton}"
                            Command="{Binding RefreshCommand}"
                            Foreground="White"
                            Margin="0,0,8,0">
                        <materialDesign:PackIcon Kind="Refresh"/>
                    </Button>
                </StackPanel>
            </Grid>
        </materialDesign:ColorZone>
        
        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Left: Users List -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Search -->
                <materialDesign:Card Grid.Row="0" Margin="0,0,0,8">
                    <Grid Margin="16">
                        <TextBox materialDesign:HintAssist.Hint="Search users..."
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </Grid>
                </materialDesign:Card>
                
                <!-- Users DataGrid -->
                <materialDesign:Card Grid.Row="1">
                    <Grid Margin="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0"
                                   Text="Users"
                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                   Margin="0,0,0,16"/>
                        
                        <DataGrid Grid.Row="1"
                                  ItemsSource="{Binding Users}"
                                  SelectedItem="{Binding SelectedUser}"
                                  Style="{StaticResource MaterialDesignDataGrid}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  IsReadOnly="True"
                                  GridLinesVisibility="Horizontal"
                                  HeadersVisibility="Column"
                                  Height="400">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Username" 
                                                    Binding="{Binding Username}"
                                                    Width="120"/>
                                <DataGridTextColumn Header="Full Name" 
                                                    Binding="{Binding FullName}"
                                                    Width="150"/>
                                <DataGridTextColumn Header="Email" 
                                                    Binding="{Binding Email}"
                                                    Width="200"/>
                                <DataGridTextColumn Header="Role" 
                                                    Binding="{Binding Role}"
                                                    Width="80"/>
                                <DataGridTextColumn Header="Status" 
                                                    Binding="{Binding IsActive}"
                                                    Width="80"/>
                                <DataGridTextColumn Header="Created" 
                                                    Binding="{Binding CreatedAt, StringFormat='{}{0:dd/MM/yyyy}'}"
                                                    Width="100"/>
                                <DataGridTemplateColumn Header="Actions" Width="150">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Style="{StaticResource MaterialDesignIconButton}"
                                                        Command="{Binding DataContext.EditUserCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        ToolTip="Edit User"
                                                        Margin="0,0,2,0">
                                                    <materialDesign:PackIcon Kind="Edit"/>
                                                </Button>
                                                <Button Style="{StaticResource MaterialDesignIconButton}"
                                                        Command="{Binding DataContext.ToggleUserStatusCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        ToolTip="Toggle Status"
                                                        Margin="0,0,2,0">
                                                    <materialDesign:PackIcon Kind="AccountToggle"/>
                                                </Button>
                                                <Button Style="{StaticResource MaterialDesignIconButton}"
                                                        Command="{Binding DataContext.ResetPasswordCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        ToolTip="Reset Password"
                                                        Margin="0,0,2,0">
                                                    <materialDesign:PackIcon Kind="Key"/>
                                                </Button>
                                                <Button Style="{StaticResource MaterialDesignIconButton}"
                                                        Command="{Binding DataContext.DeleteUserCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        ToolTip="Delete User">
                                                    <materialDesign:PackIcon Kind="Delete"/>
                                                </Button>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </materialDesign:Card>
            </Grid>
            
            <!-- Splitter -->
            <GridSplitter Grid.Column="1" 
                          HorizontalAlignment="Stretch" 
                          Background="LightGray"/>
            
            <!-- Right: Add/Edit User -->
            <Grid Grid.Column="2">
                <materialDesign:Card>
                    <Grid Margin="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Header -->
                        <StackPanel Grid.Row="0" Margin="0,0,0,16">
                            <TextBlock Text="{Binding IsEditing, Converter={StaticResource BoolToEditModeConverter}}"
                                       Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>
                            <TextBlock Text="{Binding IsEditing, Converter={StaticResource BoolToEditModeDescriptionConverter}}"
                                       Style="{StaticResource MaterialDesignBody2TextBlock}"
                                       Opacity="0.7"/>
                        </StackPanel>
                        
                        <!-- Username -->
                        <TextBox Grid.Row="1"
                                 materialDesign:HintAssist.Hint="Username"
                                 Text="{Binding Username}"
                                 IsEnabled="{Binding IsEditing, Converter={StaticResource InverseBooleanConverter}}"
                                 Margin="0,0,0,8"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        
                        <!-- Full Name -->
                        <TextBox Grid.Row="2"
                                 materialDesign:HintAssist.Hint="Full Name"
                                 Text="{Binding FullName}"
                                 Margin="0,0,0,8"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        
                        <!-- Email -->
                        <TextBox Grid.Row="3"
                                 materialDesign:HintAssist.Hint="Email"
                                 Text="{Binding Email}"
                                 Margin="0,0,0,8"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        
                        <!-- Password -->
                        <TextBox Grid.Row="4"
                                 materialDesign:HintAssist.Hint="Password"
                                 Text="{Binding Password}"
                                 IsEnabled="{Binding IsEditing, Converter={StaticResource InverseBooleanConverter}}"
                                 Margin="0,0,0,8"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        
                        <!-- Role -->
                        <ComboBox Grid.Row="5"
                                  materialDesign:HintAssist.Hint="Role"
                                  ItemsSource="{Binding UserRoles}"
                                  SelectedItem="{Binding SelectedRole}"
                                  Margin="0,0,0,8"
                                  Style="{StaticResource MaterialDesignOutlinedComboBox}"/>
                        
                        <!-- Active Status -->
                        <CheckBox Grid.Row="6"
                                  Content="Active"
                                  IsChecked="{Binding IsActive}"
                                  Margin="0,0,0,16"
                                  Style="{StaticResource MaterialDesignCheckBox}"/>
                        
                        <!-- Action Buttons -->
                        <StackPanel Grid.Row="7" Orientation="Horizontal">
                            <Button Content="{Binding IsEditing, Converter={StaticResource BoolToSaveButtonTextConverter}}"
                                    Style="{StaticResource MaterialDesignRaisedButton}"
                                    Command="{Binding IsEditing, Converter={StaticResource BoolToCommandConverter}, ConverterParameter='UpdateUserCommand|AddUserCommand'}"
                                    Margin="0,0,8,0"/>
                            <Button Content="Cancel"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    Command="{Binding ClearFormCommand}"
                                    Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        </StackPanel>
                        
                        <!-- User Info -->
                        <Border Grid.Row="8" 
                                Background="{DynamicResource MaterialDesignDividerBackground}"
                                CornerRadius="4"
                                Padding="12"
                                Margin="0,16,0,0">
                            <StackPanel>
                                <TextBlock Text="User Management"
                                           Style="{StaticResource MaterialDesignSubtitle2TextBlock}"
                                           Margin="0,0,0,8"/>
                                <TextBlock Text="{Binding Users.Count, StringFormat='Total Users: {0}'}"
                                           Style="{StaticResource MaterialDesignBody2TextBlock}"/>
                                <TextBlock Text="Admin users have full system access"
                                           Style="{StaticResource MaterialDesignBody2TextBlock}"
                                           Margin="0,4,0,0"/>
                                <TextBlock Text="User users have limited access"
                                           Style="{StaticResource MaterialDesignBody2TextBlock}"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </materialDesign:Card>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
