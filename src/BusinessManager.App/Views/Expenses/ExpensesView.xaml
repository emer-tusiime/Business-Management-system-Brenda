<UserControl x:Class="BusinessManager.App.Views.Expenses.ExpensesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="800" d:DesignWidth="1200"
             Loaded="ExpensesView_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <materialDesign:ColorZone Grid.Row="0" 
                                  Mode="PrimaryMid" 
                                  Padding="16"
                                  Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0"
                           Text="Expense Management"
                           Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                           Foreground="White"/>
                
                <StackPanel Grid.Column="1" 
                            Orientation="Horizontal">
                    <Button Style="{StaticResource MaterialDesignIconButton}"
                            Command="{Binding RefreshCommand}"
                            Foreground="White"
                            Margin="0,0,8,0">
                        <materialDesign:PackIcon Kind="Refresh"/>
                    </Button>
                </StackPanel>
            </Grid>
        </materialDesign:ColorZone>
        
        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Left: Expenses List -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Filters -->
                <materialDesign:Card Grid.Row="0" Margin="0,0,0,8">
                    <Grid Margin="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <DatePicker Grid.Column="0"
                                        materialDesign:HintAssist.Hint="Start Date"
                                        SelectedDate="{Binding FilterStartDate}"
                                        Margin="0,0,8,0"
                                        Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>
                            
                            <DatePicker Grid.Column="1"
                                        materialDesign:HintAssist.Hint="End Date"
                                        SelectedDate="{Binding FilterEndDate}"
                                        Margin="8,0,8,0"
                                        Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>
                            
                            <Button Grid.Column="2"
                                    Content="Filter"
                                    Command="{Binding SearchCommand}"
                                    Style="{StaticResource MaterialDesignRaisedButton}"/>
                        </Grid>
                        
                        <Grid Grid.Row="1" Margin="0,8,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <ComboBox Grid.Column="0"
                                      materialDesign:HintAssist.Hint="Category"
                                      ItemsSource="{Binding ExpenseCategories}"
                                      SelectedItem="{Binding SelectedCategory}"
                                      SelectedValue="{Binding FilterCategoryId}"
                                      SelectedValuePath="Id"
                                      DisplayMemberPath="Name"
                                      Margin="0,0,8,0"
                                      Style="{StaticResource MaterialDesignOutlinedComboBox}"/>
                            
                            <Button Grid.Column="1"
                                    Content="Clear"
                                    Command="{Binding CancelEditCommand}"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"/>
                        </Grid>
                        
                        <TextBox Grid.Row="2"
                                 materialDesign:HintAssist.Hint="Search by description, category, or user..."
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0,8,0,0"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </Grid>
                </materialDesign:Card>
                
                <!-- Expenses DataGrid -->
                <materialDesign:Card Grid.Row="1">
                    <DataGrid ItemsSource="{Binding Expenses}"
                              SelectedItem="{Binding SelectedExpense}"
                              Style="{StaticResource MaterialDesignDataGrid}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date" 
                                                Binding="{Binding ExpenseDate, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}"
                                                Width="140"/>
                            <DataGridTextColumn Header="Description" 
                                                Binding="{Binding Description}"
                                                Width="200"/>
                            <DataGridTextColumn Header="Category" 
                                                Binding="{Binding CategoryName}"
                                                Width="120"/>
                            <DataGridTextColumn Header="Amount" 
                                                Binding="{Binding Amount, StringFormat='{}{0:N0}'}"
                                                Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="User" 
                                                Binding="{Binding UserName}"
                                                Width="120"/>
                            <DataGridTextColumn Header="Notes" 
                                                Binding="{Binding Notes}"
                                                Width="150"/>
                            <DataGridTemplateColumn Header="Actions" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Style="{StaticResource MaterialDesignIconButton}"
                                                    Command="{Binding DataContext.EditExpenseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    ToolTip="Edit Expense"
                                                    Margin="0,0,4,0">
                                                <materialDesign:PackIcon Kind="Edit"/>
                                            </Button>
                                            <Button Style="{StaticResource MaterialDesignIconButton}"
                                                    Command="{Binding DataContext.DeleteExpenseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    ToolTip="Delete Expense">
                                                <materialDesign:PackIcon Kind="Delete"/>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </materialDesign:Card>
            </Grid>
            
            <!-- Splitter -->
            <GridSplitter Grid.Column="1" 
                          HorizontalAlignment="Stretch" 
                          Background="LightGray"/>
            
            <!-- Right: Add/Edit Expense -->
            <Grid Grid.Column="2">
                <materialDesign:Card>
                    <Grid Margin="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Header -->
                        <StackPanel Grid.Row="0" Margin="0,0,0,16">
                            <TextBlock Text="{Binding IsEditing, Converter={StaticResource BoolToEditModeConverter}}"
                                       Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>
                            <TextBlock Text="{Binding IsEditing, Converter={StaticResource BoolToEditModeDescriptionConverter}}"
                                       Style="{StaticResource MaterialDesignBody2TextBlock}"
                                       Opacity="0.7"/>
                        </StackPanel>
                        
                        <!-- Description -->
                        <TextBox Grid.Row="1"
                                 materialDesign:HintAssist.Hint="Description"
                                 Text="{Binding Description}"
                                 Margin="0,0,0,8"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        
                        <!-- Category -->
                        <ComboBox Grid.Row="2"
                                  materialDesign:HintAssist.Hint="Category"
                                  ItemsSource="{Binding ExpenseCategories}"
                                  SelectedItem="{Binding SelectedCategory}"
                                  DisplayMemberPath="Name"
                                  Margin="0,0,0,8"
                                  Style="{StaticResource MaterialDesignOutlinedComboBox}"/>
                        
                        <!-- Amount -->
                        <TextBox Grid.Row="3"
                                 materialDesign:HintAssist.Hint="Amount"
                                 Text="{Binding Amount}"
                                 Margin="0,0,0,8"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        
                        <!-- Date -->
                        <DatePicker Grid.Row="4"
                                    materialDesign:HintAssist.Hint="Expense Date"
                                    SelectedDate="{Binding ExpenseDate}"
                                    Margin="0,0,0,8"
                                    Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>
                        
                        <!-- Notes -->
                        <TextBox Grid.Row="5"
                                 materialDesign:HintAssist.Hint="Notes (Optional)"
                                 Text="{Binding Notes}"
                                 Margin="0,0,0,16"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 Height="60"/>
                        
                        <!-- Action Buttons -->
                        <StackPanel Grid.Row="6" Orientation="Horizontal">
                            <Button Content="{Binding IsEditing, Converter={StaticResource BoolToSaveButtonTextConverter}}"
                                    Style="{StaticResource MaterialDesignRaisedButton}"
                                    Command="{Binding SaveExpenseCommand}"
                                    Margin="0,0,8,0"/>
                            <Button Content="Cancel"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    Command="{Binding CancelEditCommand}"
                                    Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        </StackPanel>
                        
                        <!-- Summary -->
                        <Border Grid.Row="7" 
                                Background="{DynamicResource MaterialDesignDividerBackground}"
                                CornerRadius="4"
                                Padding="12"
                                Margin="0,16,0,0">
                            <StackPanel>
                                <TextBlock Text="Summary"
                                           Style="{StaticResource MaterialDesignSubtitle2TextBlock}"
                                           Margin="0,0,0,8"/>
                                <TextBlock Text="{Binding Expenses.Count, StringFormat='Total Expenses: {0}'}"
                                           Style="{StaticResource MaterialDesignBody2TextBlock}"/>
                                <TextBlock Text="{Binding Expenses.Sum(Amount), StringFormat='Total Amount: {0:N0}'}"
                                           Style="{StaticResource MaterialDesignBody2TextBlock}"
                                           FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </materialDesign:Card>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
